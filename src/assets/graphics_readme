The graphics inside of this directory are all directly included into the index.html file (as base64 data). The svg and ico files stored are never referenced in the source code and are only stored here for completeness.
